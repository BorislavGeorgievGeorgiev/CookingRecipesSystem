@inject NavigationManager Navigation

@code {
  protected override void OnInitialized()
  {
    Navigation.NavigateTo(
      $"{PageRoutes.Login}{PageRoutes.ReturnUrl}{Uri.EscapeDataString(Navigation.Uri)}");
  }
}
